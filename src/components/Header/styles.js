import styled from 'styled-components';

export const Wrapper = styled.header`
  padding: 16px 32px;
  display: flex;
  justify-content: flex-start;
  align-items: center;
  gap: 16px;
  position: sticky;
  z-index: 99;
`;
